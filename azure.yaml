# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: booking
services:  
  app:
    language: dotnet
    project: ./src/Aspire/Booking.AppHost/Booking.AppHost.csproj
    host: containerapp
